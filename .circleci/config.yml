version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - run: pip install -r requirements/dev.txt
      - run:
          command: calculo.py test
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
